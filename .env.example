# ──────────────────────────────────────────────
#  Relay API — Environment Configuration
#  Copy this file to .env and adjust values.
# ──────────────────────────────────────────────

# App
RELAY_APP_NAME=Relay API
RELAY_APP_VERSION=1.0.0
RELAY_DEBUG=false

# Device Mode
#   true  = in-memory mock (no hardware needed, for development/testing)
#   false = real USB hardware (relay must be plugged in)
RELAY_MOCK=false

# USB Device (DCT Tech USB Relay — ignored when RELAY_MOCK=true)
#   Vendor/Product IDs must be decimal integers.
#   0x16C0 = 5824, 0x05DF = 1503
RELAY_VENDOR_ID=5824
RELAY_PRODUCT_ID=1503
RELAY_CHANNELS=2

# Server
RELAY_HOST=0.0.0.0
RELAY_PORT=8000

# CORS (JSON array of allowed origins)
#   Use '["*"]' for development, restrict in production.
#   Example: '["http://localhost:3000","https://dashboard.example.com"]'
RELAY_CORS_ORIGINS=["*"]

# Authentication
#   Leave empty to disable API key authentication (default).
#   Set a value to require an X-API-Key header on every request.
#   Example: RELAY_API_KEY=my-secret-key-here
RELAY_API_KEY=

# Rate Limiting
#   Maximum requests per minute per client IP.
#   Set to 0 to disable (default). Recommended: 60 for production.
RELAY_RATE_LIMIT=0
